export default function Alert() {
  return (
    <div className="alert alert-warning m-0" role="alert">
      1. Store website to showcase backend skills. Do not judge on bases of UI.
      <br /> 2. Frontend is just for interacting with backend api calls. Rather
      than calling api in postman and getting data in JSON format (hard to
      read).
    </div>
  );
}
